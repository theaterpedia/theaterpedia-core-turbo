{
  "name": "turborepo-nuxt-boilerplate",
  "version": "0.0.5",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "generate": "turbo run generate",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@crearis/eslint-config-custom": "workspace:*",
    "@release-it/conventional-changelog": "^8.0.1",
    "release-it": "^17.0.1",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=v20.8.1"
  },
  "packageManager": "pnpm@7.32.5"
}
